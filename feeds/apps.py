from django.apps import AppConfig
# Konf -  Django (INSTALLED_APPS)
class FeedsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "feeds"
    verbose_name = "Feeds & Digests"
